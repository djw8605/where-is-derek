# Copy this file to .env.local and paste your Mapbox public token.
# Get one at https://account.mapbox.com/access-tokens/ (starts with "pk.").
# For deploys, set this same variable in Vercel's project settings or as a
# GitHub Actions secret named MAPBOX_TOKEN (see .github/workflows/deploy.yml).
NEXT_PUBLIC_MAPBOX_TOKEN=
